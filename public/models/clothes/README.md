# Clothing assets

## Active try-on

- **Ch36 Mixamo-skinned (preferred for Adult Male):**
  - `ready/ch36-shirt.glb` — white tee shell, skinned to `mixamorig1:*`
  - `ready/ch36-pants.glb` — dark pants shell, skinned to `mixamorig1:*`
  - Generated from `Ch36_nonPBR.fbx` via `scripts/generate_ch36_clothes.py`
  - Runtime: `skinned: true` rebinds to the live avatar skeleton

- Other catalog items under `ready/` are BlenderKit static meshes fitted via `AvatarFit` (not Mixamo-skinned).

See `ATTRIBUTION.md` for third-party credits.

